# ═══════════════════════════════════════════════════════════════
# AEL Internal Validation - Environment Variables
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ─────────────────────────────────────────────────────────────────
# Firecrawl (web scraping)
# ─────────────────────────────────────────────────────────────────
# Required for: firecrawl_search, firecrawl_map, firecrawl_extract
FIRECRAWL_BASE_URL=http://localhost:3002
FIRECRAWL_API_KEY=your-api-key

# ─────────────────────────────────────────────────────────────────
# Kafka (message queue)
# ─────────────────────────────────────────────────────────────────
# Required for: kafka_publish, kafka_list_topics, kafka_create_topic, kafka_consume
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# ─────────────────────────────────────────────────────────────────
# Ollama (ML embeddings)
# ─────────────────────────────────────────────────────────────────
# Required for: ml_embed_text, ml_text_similarity, ml_classify_text
OLLAMA_HOST=http://localhost:11434

# ─────────────────────────────────────────────────────────────────
# Filesystem tools
# ─────────────────────────────────────────────────────────────────
# Required for: fs_read, fs_write, fs_list, fs_delete
WORKSPACE_DIR=.

